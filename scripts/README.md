# split_bookmarks.py:

## Позволяет получить из одного html файла разбивку на страницы с табами и группами

[python split_bookmarks.py filepath -o output_dir_name]

"""python split_bookmarks.py filename.html -o output"""